import { Example } from './docs/Example';

# RadioToggle

<Example />

```tsx live=true
<FieldSet
  legend="Is The Room the greatest movie ever"
  name="the-room"
>
  <RadioToggleInput
    value={0}
    label="Yes"
  />
  <RadioToggleInput
    value={1}
    label="No"
  />
</FieldSet>
```

```tsx live=true
<RadioToggleInput
  value={0}
  label="Yes"
  name="demo"
/>
```

## Controlled component

Hvis du ønsker å bruke komponenten direkte som en kontrollert komponent kan det gjøres 
ved å gi hver `RadioToggleInput` en `checked` verdi og `onChange` handler.
`name` settes på fieldsetet rundt.

```tsx
const [value, setValue] = useState(1);

const changeValue = (e: ChangeEvent<HTMLInputElement>) => {
  setValue(parseFloat(e.target.value));
}

return (
  <FieldSet
    legend="Is The Room the greatest movie ever"
    name="the-room"
  >
    <RadioToggleInput
      value={0}
      checked={value === 0}
      onChange={changeValue}
      label="Yes"
    />
    <RadioToggleInput
      value={1}
      checked={value === 1}
      onChange={changeValue}
      label="No"
    />
  </FieldSet>
);
```

## Uncontrolled

Den kan også brukes som en ukontrollert komponent. Du kan gi referanse til både fieldsetet og inputene for
å ta ut verdiene.

```tsx
<FieldSet
  legend="Is The Room the greatest movie ever"
  name="the-room"
>
  <RadioToggleInput
    value={0}
    label="Yes"
  />
  <RadioToggleInput
    value={1}
    label="No"
  />
</FieldSet>
```

## Uncontrolled with react-hook-form

Det er kanskje mest interessant sammen med et form-bibliotek som  `react-hook-form`.
Da register du hvert enkelt input felt med register funksjonen. Det må ha samme navn som du gir til fieldsetet.

```tsx
const {
  register,
  handleSubmit,
} = useForm();
const onSubmit = data => console.log(data);

return (
  <form onSubmit={handleSubmit(onSubmit)}>
    <FieldSet
      legend="Is The Room the greatest movie ever"
      name="the-room"
    >
      <RadioToggleInput
        {...register('the-room')}
        value="0"
        label="Yes"
      />
      <RadioToggleInput
        {...register('the-room')}
        value="1"
        label="No"
      />
    </FieldSet>
    <PrimaryButton type="submit">
      Send inn
    </PrimaryButton>
  </form>
);
```